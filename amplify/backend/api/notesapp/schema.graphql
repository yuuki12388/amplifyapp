type ExpenseType @model {
  id: ID!
  name: String!
  description: String
}

type Expense @model {
  id: ID!
  name: String!
  description: String
  topics: [ExpenseType] @connection(keyName: "ExpenseWithTypes")
}
